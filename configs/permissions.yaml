# Tool Permission Configuration
# Canonical spec: docs/specs/tools/spec.md

permissions:
  default_policy: ask

  auto:
    - read_file
    - glob
    - grep
    - search

  plan_approval:
    - write_file
    - edit_file

  explicit_approval:
    - run_command
    - git

  high_risk: []
