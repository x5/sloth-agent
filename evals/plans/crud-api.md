# Eval Task: Create CRUD API

## Goal
Create a REST CRUD API for a "task" resource.

## Requirements
- GET /tasks - list all tasks
- GET /tasks/:id - get single task
- POST /tasks - create task
- DELETE /tasks/:id - delete task

## Constraints
- Use Python + FastAPI
- Include basic tests
- Coverage >= 80%
